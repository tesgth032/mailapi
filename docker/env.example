# 复制为 .env 后可覆盖 compose 默认值。不要把真实生产密钥提交到仓库。

MAILAPI_IMAGE=ghcr.io/tesgth032/mailapi:latest

MAILAPI_API_BIND=127.0.0.1
MAILAPI_SMTP_BIND=0.0.0.0
MAILAPI_INFRA_BIND=127.0.0.1

MAILAPI_MINIO_ROOT_USER=mailapi
MAILAPI_MINIO_ROOT_PASSWORD=mailapi-local-secret
